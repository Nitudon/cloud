namespace Cloud.GameEnums{
    
    public enum Direction{
        None,

        Left,
        Right,
    }

    public enum MoverMode{
        None,

        Tap,
        Gyro,
        Auto,
    }

    public enum PlayerMode{
        Mover,
        Supporter,
    }
}